# pytest.ini

[pytest]
testpaths = tests
addopts = --basetemp=.pytest_tmp
norecursedirs = tests/scratch
log_level = DEBUG
log_format = %(asctime)s %(levelname)-8s %(name)s %(filename)s:%(lineno)d: %(message)s
log_date_format = %H:%M:%S